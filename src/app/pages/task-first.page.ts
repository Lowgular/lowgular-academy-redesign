import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  templateUrl: './task-first.page.html',
  standalone: true,
  imports: [RouterLink],
})
export class TaskFirstPage {}
